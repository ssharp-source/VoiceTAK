// Root build.gradle
buildscript {
    repositories {
        google()
        mavenCentral()
        maven { url "https://download.atakmap.com/maven" } // ATAK SDK repo if needed
    }
    dependencies {
        classpath "com.android.tools.build:gradle:7.4.2"
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}